import {css} from '@/styled-system/css';

const carouselStyle = css({
  display: {
    base: 'flex',
    xl: 'none',
  },
  flexDirection: 'row',
  overflow: 'hidden',
});

export default carouselStyle;
