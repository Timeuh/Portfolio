import {css} from '@/styled-system/css';

const cCarousel_carousel = css({
  display: {
    base: 'flex',
    xl: 'none',
  },
  flexDirection: 'row',
  overflow: 'hidden',
});

export default cCarousel_carousel;
