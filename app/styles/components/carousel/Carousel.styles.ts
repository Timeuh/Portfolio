import {css} from '@/styled-system/css';

const carouselStyle = css({
  display: {
    base: 'flex',
    xl: 'none',
  },
  overflow: 'hidden',
  flexDirection: 'row',
});

export default carouselStyle;
